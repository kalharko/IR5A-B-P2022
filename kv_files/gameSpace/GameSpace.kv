#:kivy 2.1.0






<GameSpace>:
    map: map
    pingManager: pingManager
    tokenManager: tokenManager

    Map:
        id: map
        center: root.center
        #size: root.size

    PingManager :
        id: pingManager

    TokenManager :
        id: tokenManager
